# خطة الاختبار — نظام تتبع النقل المدرسي (سوهاج)

> **تعريف "✅ جاهز"**: بند يتحط ✅ بس لو فيه تست Playwright مكتوب ليه، اتشغّل فعليًا عن طريق GitHub Actions، والنتيجة ظهرت "Success" في الـ log. مفيش افتراضات.

---

## 1. لوجين ولي الأمر

- [x] ولي الأمر يقدر يسجل دخول برقم تليفون + كلمة سر صحيحين (`tests/login.spec.js`) — ✅ تم التأكد فعليًا (Playwright Tests #2, Success, 49s)
- [ ] رسالة خطأ واضحة لو رقم التليفون أو كلمة السر غلط
- [ ] رسالة خطأ واضحة لو رقم التليفون مش مسجل خالص
- [ ] التاب بتاع "حساب جديد" بيشتغل ويعرض الحقول الصح (تلميح ربط رقم التليفون بالمدرسة)
- [ ] بعد تسجيل الدخول الناجح، بيانات ولي الأمر (parent_auth_id) بترتبط صح بالطالب في قاعدة البيانات

## 2. لوجين السائق

- [ ] السائق يقدر يسجل دخول بنفس آلية ولي الأمر (رقم تليفون + كلمة سر)، لكن بـ `data-role="driver"`
- [ ] التبديل بين "ولي أمر" و"سائق" في `role-group` بيغيّر السلوك المتوقع صح
- [ ] رسالة خطأ واضحة لو السائق مش مسجل
- [ ] بعد الدخول، حساب السائق بيرتبط صح بالباص/المسار المخصص له

## 3. عزل البيانات بين المدارس (أولوية قصوى)

- [ ] ولي أمر من مدرسة A ميقدرش يشوف أي بيانات (طلاب، حافلات، مسارات) خاصة بمدرسة B
- [ ] سائق من مدرسة A ميقدرش يشوف أو يعدّل بيانات حافلات/مسارات مدرسة B
- [ ] RLS policies مفعّلة وبتتفحص فعليًا (مش بس مفترض إنها موجودة) على كل الجداول الحساسة
- [ ] محاولة الوصول المباشر لـ API بمعرّف (ID) مدرسة تانية بترجع خطأ صلاحيات، مش بيانات

## 4. الحضور

- [ ] تسجيل حضور/غياب الطالب من واجهة السائق أو الإدارة
- [ ] ولي الأمر يقدر يشوف سجل حضور ابنه بس (مش أي طالب تاني)
- [ ] الحضور بيتسجل بالتاريخ والوقت الصح

## 5. GPS / تتبع الحافلات

- [ ] موقع الحافلة (bus_locations) بيتحدّث ويتخزن صح
- [ ] ولي الأمر يقدر يشوف موقع الحافلة الخاصة بابنه بس على الخريطة
- [ ] تحديث الموقع بيحصل بمعدّل معقول (يتحدد لاحقًا حسب التكلفة/الأداء)

## 6. الإدارة (Admin)

- [ ] الإدارة تقدر تضيف/تعدّل مدرسة، حافلة، مسار، طالب
- [ ] الإدارة تقدر تربط سائق بحافلة ومسار محددين
- [ ] الإدارة تشوف بيانات مدرستها بس (لو فيه أكتر من admin لأكتر من مدرسة)

## 7. عام / UX

- [ ] الموقع شغال صح على المتصفح على الموبايل (الجمهور المستهدف بالكامل)
- [ ] رسائل الخطأ والنجاح كلها بالعربي وواضحة
- [ ] مفيش أي نص أو زرار متكرر يسبب "strict mode violation" في الـ selectors

## 8. جاهزية الإنتاج (قرارات المستخدم شخصيًا — مش أي AI)

- [ ] Hosting دائم (بدل GitHub Pages المؤقت لو هيتغيّر لاحقًا)
- [ ] Backup لقاعدة البيانات
- [ ] SMS OTP حقيقي بدل الإيميل الداخلي المزيّف
- [ ] Rate Limiting (خصوصًا على `phone_is_registered` المتاحة لـ anon)
- [ ] Monitoring/Alerts
- [ ] مراجعة خصوصية البيانات بشكل عام

---

## ملاحظات للحلقة الأوتوماتيكية (Groq/Gemini)

- ابدأ بالبنود الغير محددة (☐) بالترتيب من فوق لتحت، **إلا لو المستخدم حدد بند معيّن صراحة**.
- **قسم 8 ممنوع تلمسه بالكامل** — قرارات بشرية بحتة، مش للأتمتة.
- **قسم 3 (عزل البيانات) أولوية قصوى** — لو أي بند تاني (حتى لو مطلوب) هيحتاج تنازل عن عزل البيانات، توقف وافتح Issue.
